The CLI report tool needs three deliverables, and all three are required:

1. Fix the off-by-one in date-range filtering.
2. Add a `--json` output flag.
3. Document the new flag in `docs/usage.md`.

Do not modify the tests or their hash fixtures.
