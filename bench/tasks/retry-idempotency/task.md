A small payments-ledger project fails its Python test suite. Some tests pass
and some fail. Find the root cause in the production code under `bank/` and fix
it so the entire suite passes. Do not modify the tests or their hash fixtures.
